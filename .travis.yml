language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t nhj/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true nhj/react-test-app npm test

after_success:
  - docker build -t nhj/docker-frontend ./frontend
  - docker build -t nhj/docker-backend ./backend
  - docker build -t nhj/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push nhj/docker-frontend
  - docker push nhj/docker-backend
  - docker push nhj/docker-nginx

deploy:
  provider: elasticbeanstalk
  region: "us-west-2"
  app: "docker-fullstack-app"
  env: "DockerFullstackApp-env"
  bucket_name: elasticbeanstalk-us-west-2-851367625828
  bucket_path: "docker-fullstack-app"
  on:
    branch: master
  
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_ACCESS_KEY